class Solution(object):
    def findWordsContaining(self, words, x):
        """
        :type words: List[str]
        :type x: str
        :rtype: List[int]
        """
        
        ls = []
        for i in range(len(words)):
            if x in words[i]:
                ls.append(i)
        
        return ls
